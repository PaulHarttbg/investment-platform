# =================================================
# == PRODUCTION Environment Configuration
# =================================================

# -- Server Configuration --
PORT=3000
NODE_ENV=production

# -- Database Configuration (from Hostinger) --
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_unique_and_strong_db_password # IMPORTANT: Use a strong, unique password for your database.
DB_NAME=u738917511_winning_edge
DB_PORT=3306


# -- JWT & Session Secrets (Generated for you) --
JWT_SECRET= # RUN `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` TO GENERATE A NEW SECRET
JWT_EXPIRES_IN=24h
SESSION_SECRET= # RUN `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` TO GENERATE A NEW SECRET

# -- Email Configuration (ACTION REQUIRED) --
SMTP_HOST=smtp.hostinger.com
# Use port 465 for SSL or 587 for TLS
SMTP_PORT=465 
SMTP_USER=support@winningedgeinvestment.com
SMTP_PASS=your_email_or_app_specific_password # RECOMMENDATION: Use an app-specific password if your provider supports it.
FROM_EMAIL=noreply@winningedgeinvestment.com
FROM_NAME="WINNING EDGE"

# -- Security & CORS --
CORS_ORIGIN=https://winningedgeinvestment.com
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000 # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100 # Max 100 requests per 15 minutes per IP

# -- Admin Credentials --
ADMIN_EMAIL=admin@winningedgeinvestment.com
ADMIN_PASSWORD= # CRITICAL: Set a very strong, unique, and randomly generated password here. DO NOT reuse other passwords.

# -- Crypto API --
CRYPTO_API_KEY=YOUR_COINMARKETCAP_API_KEY
CRYPTO_API_URL=https://pro-api.coinmarketcap.com

# -- File Uploads & Logging --
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads
LOG_LEVEL=info
LOG_FILE=./logs/app.log